apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-api
  namespace: fcg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: users-api
  template:
    metadata:
      labels:
        app: users-api
    spec:
      initContainers:
        - name: wait-for-db
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z postgres-users 5432; do
                echo "Aguardando banco de dados users..."
                sleep 3
              done
              echo "Banco OK!"

        - name: wait-for-rabbitmq
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z rabbitmq 5672; do
                echo "Aguardando RabbitMQ..."
                sleep 3
              done
              echo "RabbitMQ OK!"

      containers:
      - name: users-api
        image: ghcr.io/11nettg30/fcg-users-api:latest
        ports:
          - containerPort: 8080
        envFrom:
          - secretRef:
              name: users-secret
          - configMapRef:
              name: users-config
